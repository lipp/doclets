extends layout.jade

block scripts
	script.
		window.onload = function() {
			document.getElementById('site-search').value = '#{query}'
		}

block content

	if doclets.length > 0
		h2= '' + doclets.length + ' doclet names match for '
			strong= query
		ul#results
			each doclet in doclets
				li.doclet
					a(href=doclet.repo.user + '/' + doclet.repo.name + '/' + doclet.repo.branch)= doclet.repo.name + ' ' + doclet.repo.branch

	else
		h2 No doclet names match for 
			strong= query

